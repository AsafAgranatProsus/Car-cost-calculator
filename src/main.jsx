import React from "react";
import { createRoot } from "react-dom/client";
import CarComparison from "./CarComparison.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CarComparison />
  </React.StrictMode>
);
